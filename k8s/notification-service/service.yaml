apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: mediflow
  labels:
    app: notification-service
    app.kubernetes.io/part-of: mediflow
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8088
