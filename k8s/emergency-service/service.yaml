apiVersion: v1
kind: Service
metadata:
  name: emergency-request-service
  namespace: mediflow
  labels:
    app: emergency-request-service
    app.kubernetes.io/part-of: mediflow
spec:
  type: ClusterIP
  selector:
    app: emergency-request-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8082
