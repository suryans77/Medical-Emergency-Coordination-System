apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: mediflow
  labels:
    app: user-service
    app.kubernetes.io/part-of: mediflow
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
