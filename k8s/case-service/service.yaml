apiVersion: v1
kind: Service
metadata:
  name: case-service
  namespace: mediflow
  labels:
    app: case-service
    app.kubernetes.io/part-of: mediflow
spec:
  type: ClusterIP
  selector:
    app: case-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8087
