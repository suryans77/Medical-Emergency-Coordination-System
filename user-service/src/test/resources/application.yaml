spring:
  main:
    banner-mode: off
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:${spring.application.name:testdb};MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

management:
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT:http://localhost:4318/v1/traces}

jwt:
  secret: ${JWT_SECRET:4A6F7264616E526F62657274735365637265744B657931323334353637383930}
  expiration: ${JWT_EXPIRATION_MS:86400000}
